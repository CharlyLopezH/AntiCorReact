import React from 'react'
import ReactDOM from 'react-dom/client'
import ACApp from './ACApp.tsx'
//import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ACApp />
  </React.StrictMode>,
)
